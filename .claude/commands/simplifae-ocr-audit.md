# simplifae-ocr-audit

Summarize OCR blockers and local OCR environment for an existing premium batch.

```bash
node tools/simplifae-ingestion/simplifae.js ocr-audit --premium-root /path/to/premium
```

Use when a batch is passing but tenders remain downgraded due to OCR.

This command does not mutate the prototype.
