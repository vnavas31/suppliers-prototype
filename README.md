# Simplifae Prototypes

This folder is prepared to be published with GitHub Pages.

Suggested repo structure:

- `/tender-detail-v1/`
- `/discovery-v1/`
- `/simple-prototype/`

Each prototype already has an `index.html`, so publishing can be done directly from this folder in a public GitHub repository.

Recommended next steps:

1. Create a public repo called `simplifae-prototypes`.
2. Upload the contents of this folder to the repo root.
3. Enable GitHub Pages from the default branch and root folder.
4. Share the generated URLs.
